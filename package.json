{
  "name": "matching-game",
  "version": "1.0.0",
  "description": "A simple card matching game (sometimes called \"memory\").",
  "main": "main.js",
  "scripts": {
    "start" : "browserify main.js > app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "game"
  ],
  "author": "ben",
  "license": "MIT",
  "dependencies": {
    "browserify": "^13.0.1"
  },
  "devDependencies": {
    "watchify": "^3.7.0"
  }
}
